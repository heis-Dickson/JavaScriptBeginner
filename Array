// Array 
var items = [ 'apple', 2.33, 56, true, 'cherry', 'banana', 'grapes'];

//Iterate or loop through this array
for(let i = 0; i < items.length; i++){
  console.log(items[i]);
}

//Alternate way to iterate through this array using for-in
for(item in items){
  console.log(item);
}
